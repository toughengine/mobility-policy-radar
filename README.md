# 모빌리티 정책·사업 브리프

자동차·모빌리티 **기술정책**과 **정부 신규사업**을 추적해 누적하는 개인용 스터디
데이터베이스 + 대시보드입니다.

이 저장소의 목적은 공고 알림이 아닙니다. 각 사업에 대해 **왜 지금 이것이 기획되었는가**를
정책적·기술적으로 분석해 쌓는 것이 핵심입니다. 정책은 결국 사업으로 실행되므로,
공고를 사후에 쫓기보다 그 앞단(업무계획 → 기술수요조사 → 예타 → 공고)을 함께 봅니다.

## 대시보드

**https://claude.ai/code/artifact/b32b2210-ac00-44df-9b8d-677817b3ac80**

어느 기기에서든 이 링크로 최신 누적 목록과 분석 카드를 볼 수 있습니다. 갱신 후에도
같은 링크로 재배포되므로 URL은 바뀌지 않습니다.

## 구성

| 경로 | 역할 |
|---|---|
| `data/policy_items.json` | 누적 DB. 정책·사업 항목 + **분석 4필드**를 append-only로 저장 |
| `data/schema.md` | 스키마 및 작성 규칙 |
| `data/inbox/` | 수집기가 만든 **검토 대기 후보** (분석 전) |
| `.github/workflows/collect-ntis.yml` | 매일 07:00 KST 자동 수집 (정책 + 공고) |
| `scripts/collect_policy.py` | **정책브리핑 보도자료 수집기 (주력)** |
| `scripts/collect_ntis.py` | NTIS 국가R&D통합공고 수집기 |
| `scripts/generate_dashboard.py` | DB → `artifact/dashboard.html` 생성기 |
| `artifact/dashboard.html` | 생성된 대시보드 (Claude Artifact로 발행) |
| `scripts/sync_from_artifact.py` | 아티팩트 → 저장소 DB 되가져오기 |
| `docs/ROUTINE_PROMPT.md` | **매일 자동 실행되는 세션의 프롬프트 전문** (트리거와 동기) |
| `docs/POLICY_BRIEFING_PLAYBOOK.md` | (구) 저장소 체크아웃 전제 절차 — `ROUTINE_PROMPT.md`로 대체됨 |

## 데이터 모델

항목마다 사실(무엇이 공고됐나)과 분석(왜 지금인가)을 분리해 저장합니다.

- **사실**: `type`(정책·계획 / 예타·예산 / 기술수요조사 / 신규사업공고 / 법·제도),
  `stage`(구상·계획 → 예타·예산확정 → 공고 → 수행중), 소관 부처, 전담기관, 공고번호,
  사업비(억원), 사업기간, 접수마감
- **분석**: `why_now`(왜 지금) · `lineage`(정책 계보) · `tech`(기술 쟁점) ·
  `implication`(시사점)

`stage`는 정부 R&D의 실제 진행 순서라서 대시보드에서 파이프라인으로 표시됩니다.
앞단 항목일수록 대응 시간이 길어 가치가 큽니다.

## 수집 경로

수집과 분석을 분리했습니다. **수집은 토큰을 쓰지 않고, 분석만 씁니다.**

| 단계 | 담당 | 시각 | 비용 |
|---|---|---|---|
| 정책브리핑 보도자료 수집 | GitHub Actions | 매일 07:00 KST | 토큰 0 |
| NTIS 국가R&D통합공고 수집 | GitHub Actions | 매일 07:00 KST | 토큰 0 |
| 정책·예타 보완 수집 | Claude 세션의 WebSearch | 08:07 KST | 검색 몇 회 |
| 분석 + 대시보드 갱신 | Claude 세션 | 08:00 KST | 신규 건수에 비례 |

그래서 매일 돌려도 부담이 없습니다. 신규 공고가 없는 날은 Actions가 커밋조차 만들지
않고, 그런 날 Claude가 할 일도 없습니다.

수동으로 돌리고 싶으면 GitHub 저장소의 **Actions 탭 → NTIS 공고 수집 → Run workflow**
를 누르면 됩니다.

### 정책브리핑을 어떻게 긁는가

- 전 부처 보도자료가 **한 목록에** 모이므로 부처별로 돌 필요가 없다. 서버 렌더링이라
  HTML만 받아도 제목·리드·newsId가 다 들어 있다.
- **최신순 훑기는 안 쓴다.** 최신 240건(약 1주일치)에 제목 매칭이 **0건**이었다.
  정책브리핑 전체에서 모빌리티 비중이 그만큼 낮다.
- 대신 **키워드 검색 + 제목 필터**를 쓴다. 검색만 쓰면 본문에 단어가 스치기만 해도
  걸려 노이즈가 크지만, 제목에 그 키워드가 실제로 있는 것만 남기면 정밀도가 확보된다.
  실측: 검색 730건 → 제목매칭 69건.
- 날짜 필터(`startDate`/`endDate`)는 GET 파라미터로 동작하지 않아 `newsId`로 중복을 거른다.

### NTIS를 어떻게 긁는가 (그리고 왜 이렇게 하는가)

- NTIS 통합공고 검색은 **서버 렌더링**이라 HTML만 받아도 공고ID·제목·소관부처·접수기간이
  전부 들어 있습니다. 상세 페이지를 열 필요가 없습니다.
- 검색 결과는 **키워드당 10건 고정**이고 최신순이 아니라 **관련도순**입니다. `자율주행`으로
  검색하면 2017년 공고가 상단에 나옵니다 (실측 0/10이 최신).
- 해결책은 **질의에 연도를 붙이는 것**입니다. `2026 자율주행`은 10/10, `2026년도 자동차`는
  8/8이 2026년 공고였습니다. 키워드 14개 × 어미 2종으로 질의를 펼칩니다.
- 같은 사업의 `(재공고)`·`(수정공고)`·`(연장공고)`는 각각 다른 ID로 올라오므로 제목을
  정규화해 접수기간이 가장 늦은 1건만 남깁니다.

### 접근이 막히는 것들

- **NTIS·IRIS·SROME는 curl로만 열립니다.** Python `requests`/`urllib`과 헤드리스
  Chromium은 연결이 리셋됩니다 (특정 TLS 클라이언트만 받는 것으로 보임).
- **WebFetch에는 환경의 egress 허용목록이 적용되지 않습니다.** 허용목록에 `*.go.kr`을
  넣어도 WebFetch는 여전히 차단되므로, Claude 세션이 NTIS를 직접 읽을 수는 없습니다.
  수집을 GitHub Actions에 맡긴 이유가 이것입니다.
- 빅카인즈 OpenAPI는 기관 업무협약·승인이 전제라 개인 자격으로는 사실상 어렵습니다.
- 나라장터·부처 보도자료 API는 검토했지만 채택하지 않았습니다 (용역 입찰 위주라 노이즈가
  크고, 보도자료 API는 모빌리티 주무부처가 빠져 있음). 관련 코드는 git 히스토리에 있습니다.

## 수동 실행

```bash
# 대시보드 재생성 (DB를 직접 편집한 뒤)
python3 scripts/generate_dashboard.py

# NTIS 수집을 직접 돌려보기
python3 scripts/collect_ntis.py            # 올해 공고
python3 scripts/collect_ntis.py --stdout   # 파일로 저장하지 않고 출력만
```

수집기는 `data/inbox/ntis-YYYY-MM-DD.json`에 **후보만** 저장합니다. 분석은 판단이
필요하므로 Claude 세션이 후보를 검토해 `data/policy_items.json`으로 승격시킵니다.

## 자동화 방식 — 정본은 저장소가 아니라 아티팩트에 있다

이 부분은 직관에 반하므로 먼저 짚습니다. **일일 자동화의 DB는 저장소가 아니라 발행된
대시보드 HTML 안**입니다.

이유는 하나입니다. Routine이 깨우는 세션에는 **저장소 체크아웃도 GitHub MCP 도구도
없습니다**(2026-09-09 실측, 2026-09-14 재확인). 그 세션이 쓸 수 있는 것은 `Bash`(curl·
python3)·`Artifact`·`WebSearch`·`PushNotification` 뿐입니다. 저장소에 손이 닿지 않으니
저장소를 DB로 쓸 수가 없습니다.

그래서 대시보드 HTML에 마커를 박아 두고 그 사이를 DB로 씁니다:

```
const ITEMS = /*ITEMS_JSON_START*/[ …항목 배열… ]/*ITEMS_JSON_END*/;
<!--TOTAL_COUNT_START-->84<!--TOTAL_COUNT_END-->
<!--GENERATED_AT_START-->2026-09-14 21:46 KST<!--GENERATED_AT_END-->
```

대시보드는 이 배열만 보고 전부 클라이언트에서 그리므로, 마커 세 곳만 갈아끼우면
저장소 없이도 갱신이 끝납니다.

### 하루의 흐름

| 시각 | 누가 | 무엇을 | 비용 |
|---|---|---|---|
| 07:00 KST | GitHub Actions | 정책브리핑 + NTIS 수집 → `data/inbox/` 커밋 | 토큰 0 |
| 08:07 KST | Claude Routine 세션 | 아티팩트 읽기 → curl 수집 → 선별 → **분석** → 아티팩트 재발행 | 신규 건수에 비례 |
| 사람이 있을 때 | 이 저장소 | `scripts/sync_from_artifact.py`로 아티팩트 → DB 되가져오기 | — |

Routine 세션의 프롬프트 전문은 `docs/ROUTINE_PROMPT.md`에 있습니다. **프롬프트를 고칠
때는 그 파일과 트리거를 함께 고칩니다.** 수집 스크립트가 프롬프트 안에 통째로 들어 있는
것도 같은 이유입니다 — 그 세션에는 스크립트를 담을 저장소가 없습니다.

### 신규 0건인 날에도 재발행한다

갱신 시각만 바꿔 내보냅니다. 그러지 않으면 대시보드에서 "오늘 점검했고 새 것이 없었다"와
"수집기가 죽었다"가 똑같아 보입니다. 실측상 신규 0건은 흔한 날입니다.

수집이 실제로 고장난 경우는 두 신호로 구분합니다. `queries_ok`가 0이면 네트워크 차단,
`queries_ok`는 정상인데 `searched`가 0이면 정책브리핑 마크업 변경에 의한 정규식 파싱
붕괴입니다. 둘 다 **발행하지 않고** 알립니다.

### 되돌림 (sync)

정본이 아티팩트로 가면 `data/policy_items.json`이 뒤처집니다. git 이력이 있어야 "언제
무엇이 늘었는지"를 되짚을 수 있으므로 이 간격은 메워야 합니다:

```bash
# Artifact 도구로 index.html 을 내려받은 뒤
python3 scripts/sync_from_artifact.py <내려받은 index.html> --check   # 차이만 확인
python3 scripts/sync_from_artifact.py <내려받은 index.html>          # 반영
python3 scripts/generate_dashboard.py                                # 저장소 HTML도 맞춤
```

아티팩트 쪽 항목이 저장소보다 **적으면 쓰지 않고 멈춥니다.** 자동화가 항목을 잃은 채
발행한 사고라면 그것을 저장소에까지 덮어쓰면 안 되기 때문입니다.

## 두 번째 레이더 — 연구용역 (수주용)

같은 저장소에 있지만 **목적이 다른 별개 파이프라인**입니다. 정책 브리프가 "왜 지금 이
정책인가"를 공부하는 것이라면, 이쪽은 **내가 수주할 수 있는 공공 연구용역을 마감 전에
찾는 것**입니다. 그래서 analysis 4필드가 없고, 마감·추정가격·업종제한이 전부입니다.

**대시보드: https://claude.ai/artifact/EgDREdFtTJP2QpEezUwyep**

| 경로 | 역할 |
|---|---|
| `scripts/collect_bids.py` | 나라장터 용역 입찰공고 수집기 (apis.data.go.kr) |
| `scripts/collect_reports.py` | NKIS 국책연구기관 연구보고서 수집기 (참고자료 탭) |
| `scripts/generate_bid_dashboard.py` | `data/bids.json` → `artifact/bid_dashboard.html` |
| `docs/BID_ROUTINE_PROMPT.md` | 매일 07:30 KST 갱신 세션의 프롬프트 전문 |
| `data/bids.json` | 누적 공고 DB |
| `data/reports.json` | 누적 참고자료 DB |

### 소스를 나라장터 하나로 고른 이유 (2026-09-23 실측)

공공기관은 일정 금액 이상 용역을 **법적으로 나라장터에 공고해야 해서** 부처·지자체·
공공기관·출연연·테크노파크 발주가 전부 한곳에 모입니다. 도달성은 이렇습니다:

| 소스 | 결과 (각 8회 재시도) |
|---|---|
| `apis.data.go.kr` 나라장터 입찰공고 API | ✅ 열림 — 인증키로 정상 응답 |
| `g2b.go.kr` 웹 | ❌ 8회 전부 실패 |
| `prism.go.kr` (정책연구용역 종합관리시스템) 웹 | ❌ 8회 전부 실패 |
| KIET · KISTEP · KOTI 홈페이지 | ✅ 열림 (2층 보완 소스로 여지 있음) |

### 거르는 방법

7일치 3,424건 중 대상은 100여 건뿐이라 2단으로 거릅니다.

1. **조달 대분류**로 1차 — 기술용역(토목 설계·감리) 905건, 폐기물 190건, 시설관리 등은
   통째로 제외. 핵심은 **연구조사서비스**(학술연구서비스 266).
2. **제목 키워드**로 2차 — 대분류만으로는 부족합니다. 「연구조사서비스」 안에도
   GAP토양용수 안전성 분석, Round Robin Test 같은 **시험·분석 용역**이 섞여 있습니다.
   반대로 POS 키워드만 쓰면 '기본계획·종합계획'이 소하천정비·하수도정비를 대량으로
   끌고 옵니다. NEG가 그만큼 중요합니다.

결과는 126건/7일(하루 18건)입니다.

### 업종제한으로 거르지 않는 이유

`indstrytyLmtYn`은 표본에서 **Y 605 / N 387** — 39%가 제한이 아예 없습니다. 수집 단계에서
Y를 버리면 회사가 마침 그 업종을 등록한 건까지 날아갑니다. 거르지 말고 화면에 배지로
띄워 사람이 판단하게 합니다.

### 참고자료 탭 — 왜 PRISM이 아니라 NKIS인가

「정책연구용역 결과보고서를 참고하고 싶다」는 요구에서 출발했습니다. 실측 결과:

| 소스 | 결과 |
|---|---|
| `prism.go.kr` (정책연구용역 종합관리시스템) | ❌ 세션 3회에 걸쳐 **18회 재시도 전부 실패** |
| `www.data.go.kr` 포털 | ❌ 실패 (API 게이트웨이 `apis.data.go.kr`만 열림) |
| **`www.nkis.re.kr`** (국가정책연구포털) | ✅ 열림 |

NKIS는 경제·인문사회연구회 소관 **26개 국책연구기관**(산업연구원·과학기술정책연구원·
교통연구원·에너지경제연구원·국토연구원·KDI 등)의 연구보고서가 모이는 곳이라, 선행연구를
찾는 목적에는 오히려 더 맞습니다. **다만 PRISM과 완전히 겹치지는 않습니다** — 국책연구기관이
수탁연구보고서로 올린 것은 잡히지만 민간이 수행한 부처 용역 결과물은 빠질 수 있습니다.

엔드포인트도 실측으로 찾았습니다. 화면 목록은 AJAX라 안 긁히는데,
**`/newestExcelDown.do?listPerPage=500&currentPage=N`이 인증 없이 JSON을 그대로 돌려줍니다**
(엑셀 내려받기용). 검색어 파라미터는 무시되므로 최신 4페이지(2,000건)를 받아 제목·초록으로
거릅니다 — 613건이 남습니다. 상세 페이지는 GET으로 안 열려
`/totalSearchResults.do?searchWord=<제목>`을 링크로 씁니다(동작 확인).

### 자동차·모빌리티 배지는 제목에만 겁니다

초록까지 보면 「수소공급 믹스 분석」·「탄소중립도시 공간계획」처럼 본문에 수소·교통이
스치기만 한 보고서에 배지가 붙습니다. 실측에서 192건이 붙었다가 제목 기준으로 좁히니
**36건**으로 줄었고, 남은 것은 전부 진짜 모빌리티 연구입니다.

### D-day는 브라우저에서 계산합니다

재발행이 하루 늦어도 남은 일수는 정확해야 하므로 서버에서 굳히지 않습니다. 그리고
**시분 차가 아니라 달력 날짜 차**입니다 — `ceil((마감-지금)/하루)`를 쓰면 23시간 전에
끝난 공고가 D-0으로, 1시간 뒤 마감인 공고가 D-1로 나옵니다(실제로 겪은 버그).

## 연혁 — 그리고 이 저장소가 아닌 것

이 프로젝트는 `toughengine/TEST1`의 「모빌리티 브리프」(일일 뉴스 수집)에서 출발해
정책·사업 추적으로 컨셉을 전환한 뒤, 별도 저장소로 분리했습니다.

**두 프로젝트는 완전히 별개입니다. 저장소도 아티팩트도 다릅니다.**

| | 모빌리티 브리프 (뉴스) | 모빌리티 정책·사업 브리프 (이 저장소) |
|---|---|---|
| 저장소 | `toughengine/TEST1` | `toughengine/mobility-policy-radar` |
| 아티팩트 | [688798f0…](https://claude.ai/code/artifact/688798f0-9f72-4476-b4f9-119c130b689a) 🚗 | [b32b2210…](https://claude.ai/code/artifact/b32b2210-ac00-44df-9b8d-677817b3ac80) 🏛️ |
| 대상 | 국내·글로벌 모빌리티 산업 뉴스 | 정부 기술정책과 신규 R&D 사업 |
| 상태 | 중단됨 (검색 신선도·도메인 차단 문제) | 운영 중 |

> **주의 — 다른 세션에서 작업할 때**
> 전환 초기에 두 컨셉이 **아티팩트 URL 하나를 공유**했습니다. 그 결과 정책 대시보드가
> 「모빌리티 브리프」 링크를 덮어썼고, 이를 되돌리려던 다른 세션이 이 저장소 전체를
> 뉴스 컨셉으로 되돌리는 일이 있었습니다 (PR #1, 2026-09-08).
> 지금은 아티팩트가 분리되었으니, **이 저장소는 `b32b2210…`에만 발행**하고
> `688798f0…`은 건드리지 마세요. 반대도 마찬가지입니다.

## 주의

대시보드의 분석 필드는 공개 자료를 근거로 한 **해석**이며 정부의 공식 입장이 아닙니다.
의사결정에 쓰기 전에 반드시 원문 링크로 교차 확인하세요.
